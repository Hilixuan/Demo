# 制造业 · 工业 · 生物医药 AI Agent — Agent 核心规则

> 跨客户端（Cursor / Codex / WorkBuddy）canonical 核心约束。详细 PM 流程、目录约定与设计规范见各专项文件。

## 项目定位

本项目用于**产品经理**在制造业、工业、生物医药领域进行 AI Agent / AI 产品的需求分析、功能设计、方案输出和交付评审，**不直接修改生产代码**。

工作产物包括但不限于：AI 用例拆解、产品功能设计文档、交互原型（HTML）、方案 PPT、行业调研报告。覆盖行业：
- **制造业**：智能工厂、生产运营、工业视觉、预测性维护、智能排产、数字孪生
- **工业**：设备运维、EHS 安全、能源管理、物流供应链优化
- **生物医药**：药物研发 AI、临床试验优化、合规智能体、GMP 生产质控

## 目录与定稿

- 新任务和迭代默认写入 `.session-workspace/[模块名]/`。
- 只有用户明确说「定稿 / 可交付 / 移入设计成果」后，才可迁入 `设计成果/`。
- 目录结构、命名、定稿判断与清理细则见 `.cursor/rules/project-workspace.mdc`（若存在）。

## 工作方式

- 新模块或大改：默认先诊断现状、梳理行业背景和问题范围，获得用户确认后再大面积制作原型或方案。
- 符合以下任一情况视为「大改」：新增完整业务模块或 AI Agent 产品线；修改 3 个及以上功能模块；改变核心 AI 决策流程或状态流转；涉及跨模块业务闭环；需要新增或改变接口/模型能力。单页文案、样式或局部交互微调可直接进入原型阶段。
- 已确认的产品决策（需求说明书正文、`STATUS.md` 决策记录）不得擅自修改。
- 用户说「不要动原型」→ 只改文档；用户说「继续」→ 先读模块 `STATUS.md` 与相关 docs。

## 行业 AI 场景拆解规范（新增）

当用户提出一个行业场景时，按以下范式进行 AI 产品分析：

### 场景分析四问

1. **痛点**：该场景当前人力/传统系统瓶颈是什么？
2. **数据**：需要什么数据才能让 AI 跑起来？（传感器、工单、影像、文本）
3. **AI 能力**：需要哪种 AI？（CV 视觉 / NLP 文本 / 时序预测 / 约束优化 / 生成式 AI / Agent 多步推理）
4. **价值**：量化指标是什么？（效率提升 %、成本降低 %、准确率提升 %）

### AI Agent 特别考量

区别于传统软件功能，AI Agent 产品需额外关注：
- **自主决策边界**：Agent 能做主到什么程度？哪些必须人工确认？
- **模型选型依据**：为什么选大模型 vs 小模型 vs 规则引擎？
- **工具调用链**：Agent 需要调用哪些外部系统/工具？
- **幻觉风险与护栏**：输出不可靠时的降级策略
- **人机协同模式**：Human-in-the-loop / Human-on-the-loop / Fully Autonomous？
- **MLOps 闭环**：模型如何持续训练、评估、部署？

## 需求说明书编写（默认遵循）

用户未特别声明时，按以下规则撰写与维护需求说明；**无需用户每轮重申**。

### 文档组织

| 规则 | 说明 |
|------|------|
| 工作区两文件制 | `.session-workspace/[模块]/` 只维护 `STATUS.md` 与 `docs/[模块]-需求说明书.md` 两类管理文档 |
| 需求说明书一致 | 工作区模块需求说明书与版本主需求说明书名称一致、正文结构一致；定稿时将模块正文并入版本主文档对应章节 |
| 版本唯一主文档 | 每个版本在 `设计成果/[版本]/docs/[版本]-需求说明书.md` 维护**一份**统一需求说明书 |
| 禁止散落单册 | 不在 `设计成果/` 各模块子目录另建 `*-需求说明书.md`；新模块以**章节**并入主文档 |
| 过程信息归属 | 诊断结论、待确认事项、决策轨迹、下一步只写入 `STATUS.md`，不进入需求说明书 |
| 版本入口 | `设计成果/[版本]/index.html` 的「需求文档」区只链主文档，不链模块级需求文件 |

### 受众与粒度

面向**产品经理、技术负责人与评审**，写清「做什么、怎么展示、怎么交互、怎么算」，供原型对齐与研发理解业务。

**每章至少覆盖**（按模块实际需要取舍，不可只有一两段概述）：

1. **业务目标与范围**：解决什么问题、谁在用、行业背景、纳入本期与不在本期的边界
2. **页面关系 / 信息结构**：入口、跳转、弹窗/子页层级、跨页闭环（可用列表或示意）
3. **展示字段**：按页面覆盖列表列、表单项、卡片、弹窗字段；表格写明字段含义、展示格式与展示条件
4. **交互逻辑**（含 AI 决策流程）：写明触发入口、前置条件、AI 推理步骤、操作结果、状态变化、校验顺序、成功/失败反馈、空态与错误态、AI 建议 vs 人工决策边界
5. **业务规则**：计算口径、状态判定、排序与去重、权限、阈值、上下限及异常边界；AI 模型相关规则（置信度阈值、模型版本、降级策略）
6. **接口与数据（功能级）**：仅写 VO 扩展字段、接口能力变更、前后端约定；AI 模型接口（输入输出 schema）可在此说明。**不写**表结构、DDL、字段类型除非用户明确要求
7. **AI 模型与 Agent 设计（新增）**：模型选型、Prompt 设计要点、工具调用列表、多 Agent 协作拓扑（如有）、评估指标与监控

### 章节结构模板

工作区模块需求说明书固定使用：

```markdown
# [模块] · 需求说明书

| 项目 | 说明 |
|------|------|
| 文档版本 | v0.1 |
| 更新日期 | YYYY-MM-DD |
| 状态 | 草案 / 已确认 / 已定稿 |
| 行业 | 制造业 / 工业 / 生物医药 |
| 端 | Web / App / 双端 |
| 关联原型 | ... |

## 一、业务目标与范围
## 二、页面关系 / 信息结构
## 三、展示字段
| 页面/区域 | 字段 | 含义 | 展示格式/条件 |
|-----------|------|------|---------------|

## 四、交互逻辑（含 AI 决策流程）
| 页面/触发入口 | 前置条件 | AI推理步骤 | 操作与校验 | 状态变化/反馈 |
|---------------|----------|------------|------------|---------------|

## 五、业务规则
| 规则项 | 规则口径 | AI相关 | 权限/边界 |
|--------|----------|--------|-----------|

## 六、接口与数据（功能级，可选）
## 七、AI 模型与 Agent 设计（可选）
## 八、不在本期（可选）
```

### 与原型分工

| 交付物 | 侧重 |
|--------|------|
| 需求说明书 | 字段、规则、链路、AI 决策流程、文案口径 |
| HTML 原型 | 布局、组件态、可点击演示、AI 交互演示 |
| `index.html` | 总览 + 评审双模式入口；总览仅展示 `MODULES.overview` |

需求说明书须能**脱离原型**读懂业务和 AI 逻辑；原型须能**对照说明书**验收关键字段与交互。

---

## 版本评审 index 设计（默认遵循）

（保留原 AGENTS.md 中评审 index 的完整设计，不作实质性行业变更，以下仅做适配标注）

### 双模式（必须）

| 模式 | 内容 |
|------|------|
| **总览模式** | 各模块需求概述卡片（标题 + 行业标签 + 端标签 + 两句话 `overview`）；顶部「去评审」；底部「需求文档」列表 |
| **评审模式** | 左侧按模块分组；**分组标题右侧**用两个小图标切换「原型 / 需求说明书」；右侧对应预览 |

### MODULES 约定

每模块含：`id`、`title`、`industry`（制造业/工业/生物医药）、`tag`/`tagLabel`、`overview`、`pages_nav[]`、`docs[]`。

### overview 撰写要求

- **两句话**：第一句说清「做什么 / 解决什么行业问题 / AI 化价值」，第二句补充「本期范围或关键行为」
- 不写规则条目、演示路径、字段清单、涉及页面枚举

示例：
- ✅ `基于工业视觉的产线缺陷检测 Agent，替代人工灯检工位。支持实时判废、缺陷分类与品质报表自动生成。`
- ❌ `检出率 99.5%、误报率 2%、演示：上传图片检测`

---

## 设计风格

默认**专业工业/B端工具型风格**：清晰的信息层级、合理的留白、符合行业用户的操作习惯。不擅自大幅视觉重构、深色玻璃风或强炫技动效。

### 默认视觉母版：浅色数字孪生工厂驾驶舱

当用户要求生成制造业/工业类 Demo 演示系统、经营驾驶舱、项目看板、工厂运营平台、设备监控、方案报价协同或类似 B 端原型，且未提供更强视觉参考时，默认采用**浅色数字孪生工厂驾驶舱**风格：

- **整体气质**：浅色科技工业数据大屏 / 工厂运营驾驶舱；白底、蓝色光感、轻 3D 工业可视化，高信息密度但低压迫感。
- **关键词**：浅色工业驾驶舱、科技蓝数据中台、轻拟物 3D 工厂可视化、卡片化运营看板、数据闭环流程、蓝白冷色系、弱边框、柔和阴影。
- **色彩基准**：主色优先使用亮科技蓝 `#3f7df6` / `#4f83f1`；深蓝标题 `#153b7a` / `#1f4b8f`；背景 `#f6f9ff`；卡片 `#ffffff` / `#f8fbff`；边框 `#dbe7fb`；成功绿 `#46c58a`；告警橙 `#f5a623`；异常红 `#ff6b4a`；辅助紫 `#7b61ff`。
- **布局骨架**：优先 1440 宽桌面演示；三栏或主次分区布局，左侧放核心指标/进度/成本，中间放流程链路和主视觉，右侧放设备状态/库存/预警；底部可放趋势图、闭环流程或能力链路。
- **视觉重心**：制造业/工业看板应尽量设置一个中心主视觉，可为等距 3D 工厂、产线、设备拓扑、数字孪生场景、流程链路或系统闭环图；主视觉周围配数据浮窗。
- **组件语言**：白色或浅蓝卡片，8-12px 圆角，浅蓝灰细边框，轻阴影；指标卡采用「图标 + 指标名 + 大数字 + 环比」；流程节点采用圆形浅蓝图标底和虚线箭头；状态列表采用小图标 + 文案 + 标签 + 时间。
- **图表规则**：图表使用细线、圆点、浅网格、低装饰；环形图以蓝色为主，绿/黄/红作为状态分段；红色只用于真实异常，不做大面积铺色。
- **排版规则**：中文优先 `PingFang SC` / `Microsoft YaHei`；标题 14-16px 深蓝加粗，正文与图表标签 10-12px，关键数字加大加粗；层级依靠字号、颜色深浅、卡片分区和留白建立，不使用夸张标题。
- **底部闭环表达**：涉及数据平台、经营分析或 AI Agent 时，可在底部加入「数据实时采集 → 数据清洗融合 → 数据可视化分析 → 智能决策支持 → 业务闭环优化」等闭环链路，但文案需贴合当前业务场景。
- **避免事项**：避免深色玻璃风、霓虹炫技、巨大渐变背景、过度动效、低信息密度营销页、纯装饰图形、没有业务含义的图标堆叠。

行业相关视觉建议：
- **制造业/工业**：默认采用浅色数字孪生工厂驾驶舱；只有用户明确要求中控室/夜间大屏/深色主题时，才可切换为暗色数据看板；始终保持清晰的告警色标
- **生物医药**：白色/浅灰主调、合规信息高亮、清晰的审核链路视觉引导

仅当用户明确说「**自由发挥**」「大胆美学」「创意发挥」「不拘现网」等时，才切换到大胆美学模式（见 `frontend-design` Skill）。

---

## AI 页面 UI 设计品质规范（默认遵循）

> 本规范与 `impeccable` / `awesome-design-md` Skill 协同工作。

**执行规则**：

1. **加载 Skill 优先**：凡涉及生成 HTML 原型、Demo 页面、演示系统、管理看板等可视化产物时，必须先通过 `Skill` 工具加载 `impeccable`，将其设计系统约束注入当前会话。
2. **本规范为硬底线**：当 Skill 因故不可用时（未安装、加载失败），以本规范为硬约束执行；当 Skill 可用时，Skill 注入的设计 Token 与本规范叠加，冲突处以 Skill 为准。
3. **风格切换**：如需切换视觉风格或做多方案对比，加载 `awesome-design-md` 选择参照品牌设计语言（如 Linear / Notion / Stripe / Datadog 等）。

凡生成可视产物，必须严格遵守以下规则。

### 色彩体系

**主色与功能色**
| 用途 | 色值 | 说明 |
|------|------|------|
| 主色（品牌蓝） | `#1a73e8` | 按钮、链接、选中态、主要图标 |
| 主色浅底 | `#e8f0fe` | 选中背景、高亮卡片 |
| 主色深 | `#1557b0` | hover/active 加深 |
| 成功 | `#10b981` | 正常、已完成、通过 |
| 告警 | `#f59e0b` | 待确认、注意、进行中 |
| 错误/风险 | `#ef4444` | 告警、异常、超期 |
| 信息 | `#3b82f6` | 辅助信息、中性状态 |

**中性色（禁止使用 #999 / #ccc / #ddd 等无温度灰色）**
| 用途 | 色值 |
|------|------|
| 页面背景 | `#f8fafc` |
| 卡片/面板背景 | `#ffffff` |
| 边框/分割线 | `#e2e8f0` |
| 次要背景（表头/斑马纹偶数行） | `#f1f5f9` |
| 文字主色（标题/正文） | `#0f172a` |
| 文字次色（说明/元数据） | `#64748b` |
| 文字弱色（占位符/禁用文字） | `#94a3b8` |

### 字体层级

| 层级 | 字号 | 字重 | 行高 | 使用场景 |
|------|------|------|------|----------|
| H1 页面标题 | 20px | 700 | 1.3 | 页面顶部大标题 |
| H2 卡片标题 | 16px | 600 | 1.4 | 各卡片/区块标题 |
| Body 正文 | 14px | 400 | 1.6 | 表格内容、段落、表单 |
| Caption 辅助 | 12px | 400 | 1.4 | 标签、时间戳、元数据 |
| Data 数据 | 14px | 600 | 1.4 | KPI 数字，启用 `tabular-nums` 等宽数字 |
| 最小字号 | — | — | — | **禁止使用小于 12px 的字号** |

### 间距系统（基于 8px 网格）

| 间距等级 | 数值 | 使用场景 |
|----------|------|----------|
| xs | 4px | 图标与文字间距、同类标签间距 |
| sm | 8px | 同类元素之间、表单项之间的最小间距 |
| md | 16px | 卡片内分区间距、表单项组间距 |
| lg | 24px | 卡片之间、页面区块之间 |
| xl | 32px | 页面内容与边缘距离 |
| 卡片内边距 | 20px | 卡片/面板内部 padding |

**禁止使用 5px / 10px / 15px / 25px 等非 8px 倍数的间距值。**

### 圆角与阴影

| 元素 | 圆角 | 阴影 |
|------|------|------|
| 按钮 / 标签 / 徽章 | 6px | 无 |
| 输入框 / 下拉框 | 8px | 无（focus 时 2px 主色外发光） |
| 卡片 / 面板 | 10px | `0 1px 3px rgba(0,0,0,.06), 0 1px 2px rgba(0,0,0,.04)` |
| 悬浮态（hover 卡片） | 10px | `0 4px 16px rgba(0,0,0,.08)` |
| 弹窗 / 模态框 | 12px | `0 8px 32px rgba(0,0,0,.12)` |

### 交互状态（每个可交互元素必须定义）

| 状态 | 规则 |
|------|------|
| **normal** | 默认样式，无额外效果 |
| **hover** | 背景叠加 4% 透明度深色层，cursor: pointer，过渡 150ms ease |
| **active / pressed** | `transform: scale(0.98)`，背景加深 |
| **disabled** | opacity: 0.4，cursor: not-allowed |
| **focus** | 2px 主色外发光 `box-shadow`，不移位（无 outline offset） |

所有过渡统一使用 `transition: all 150ms cubic-bezier(0.4, 0, 0.2, 1)`，禁止使用弹跳、翻转、旋转等装饰性动画。

### 表格规范

- 表头：背景 `#f1f5f9`，字号 12px，颜色 `#64748b`，字体大写，左对齐
- 数据行：行高 44px，偶数行背景 `#fafbfc`（斑马纹）
- 行 hover：背景 `#e8f0fe`
- 最后一行无底边框
- 数字列：右对齐，启用 `font-variant-numeric: tabular-nums` 等宽数字
- 空表格不展示空行，展示居中的空态插画/图标 + 引导文案

### 状态标签规范

| 语义 | 背景色 | 文字色 | 示例 |
|------|--------|--------|------|
| 风险 / 告警 / 延期 | `#fee2e2` | `#ef4444` | 🔴 高风险 |
| 待确认 / 进行中 | `#fef3c7` | `#d97706` | 🟡 待审批 |
| 正常 / 已完成 | `#d1fae5` | `#059669` | 🟢 已完成 |
| 信息 / 草稿 | `#e8f0fe` | `#1a73e8` | 🔵 草稿 |

格式：圆角 12px（胶囊形），字号 11px，内边距 2px 8px。

### 空态与异常态

- **无数据时**：不展示空表格或空白区域，展示居中空态插画/图标 + 简短引导文案 + 可选操作按钮（如"新建"或"导入"）
- **加载中**：使用骨架屏（shimmer 动画），不用全屏 loading 遮罩
- **错误态**：简短错误描述 + 重试按钮；不影响页面其他区域

### 图表配色序列（柱状图/折线图/饼图）

```
#1a73e8, #34a853, #fbbc04, #ea4335, #8b5cf6, #14b8a6, #f97316, #6366f1
```

- 饼图/环形图从蓝色开始顺时针排列
- 红色（#ea4335）仅用于标注真实异常，不做大面积铺色
- 图表使用细线、圆点、浅网格、低装饰

---

## Skills 与工具

- 默认使用**最少且足够**的 Skills：一个主 Skill，必要时增加一至两个辅助 Skill。
- 不同时使用方向明显冲突的 Skills（如大幅视觉重构类与现网对齐类）。
- 插件与 MCP 仅在需要读取或写入对应外部服务时使用；不因已安装而自动调用。
- **设计 Skill 强制加载规则**：凡涉及生成 HTML 原型、Demo 页面、演示系统、管理看板、数据驾驶舱等可视化产物时，必须加载 `impeccable` Skill 注入设计系统约束；如需切换视觉风格或做方案对比，可额外加载 `awesome-design-md` 选择参照设计语言。
- **UI/UX 设计增强规则**：凡涉及页面风格设计、视觉系统选择、配色、字体、图标、图表、动效、可访问性、响应式或 UI 质量评审时，优先使用 `ui-ux-pro-max`（若已安装/可用）生成/校验设计系统建议；它只用于 UI/UX 决策和质量检查，不用于纯后端、接口、数据库或非视觉脚本任务。生成新页面时优先运行其 `--design-system`，看板类页面建议使用高密度参数；若用户要求「自由发挥」「大胆美学」「创意发挥」「不拘现网」或需要摆脱模板感，再叠加 `frontend-design` 做差异化视觉方向与设计自审，但不得覆盖已确认的产品决策和既定行业/B端约束。
- **需求整理增强规则**：当用户提供杂乱需求清单、会议纪要、功能点堆叠、页面想法或需要拆分页面功能点时，可优先使用 `product-requirements`（若已安装/可用）进行需求澄清、页面功能拆分、验收口径整理与 PRD 初稿标准化；若该 Skill 未安装或不可用，则按本文件「需求说明书编写」章节兜底执行，并明确区分诊断过程信息与正式需求正文。

### Skill 路由

| 场景 | 主 Skill / 工具 |
|------|-----------------|
| 行业场景诊断 & AI 用例拆解 | `ai-pm-manufacturing` |
| 杂乱需求清单标准化 / 页面功能点拆分 / PRD 初稿整理 | `product-requirements`（若未安装/不可用则按本文件 PRD 规范兜底，明确区分过程信息与正式需求正文） |
| 竞品分析 & 市场调研 | `market-researcher` |
| 行业解决方案 PPT 生成 | `solution-ppt-generator-v3` |
| 新建或修改 HTML 原型 / Demo 页面 | 本机直接生成 + **必须加载 `impeccable`** |
| 切换 UI 视觉风格 / 多方案对比 | **加载 `awesome-design-md`** 选择参照品牌设计语言 |
| 页面风格设计 / 配色字体 / 图标图表 / 响应式与 UX 规则检索 | `ui-ux-pro-max`（UI/UX 决策和质量检查；生成新页面优先 `--design-system`，看板建议高密度参数） |
| 原型评审前验证 / UI 质量走查 | `impeccable`（Design QA） |
| 定稿前视觉精修 | `impeccable`（优先）或 `product-ui-refine` |
| 用户明确要求自由发挥 / 大胆美学 / 创意发挥 / 不拘现网 | `frontend-design`（必要时先用 `ui-ux-pro-max` 生成基础设计系统；大胆设计仍须服务业务场景、信息层级和评审可读性） |
| 数据分析 & KPI 报告 | `project-analysis-report` |
| 行业研究报告生成 | `fbs-bookwriter` |
| 制造业行业知识库查询 | 必要时 `WebSearch` + 行业垂直数据源 |

## 浏览器验证协作约定

- 在本项目中，凡涉及打开内置浏览器进行原型验证、截图、交互检查、响应式检查或页面巡检时，Agent **不得自行尝试启动或打开内置浏览器**。
- Agent 应先完成可离线执行的制作、修改和静态检查；到需要浏览器验证时，暂停并明确告知用户需要打开的页面地址（`file://` 路径或本地服务 URL）。
- 用户手动打开内置浏览器并进入指定页面后，会回复确认；Agent 收到确认后再继续执行浏览器截图、交互、视觉和响应式验证。
- 若页面需要本地服务，Agent 可以先启动或说明需要启动的服务与端口，但仍由用户负责在内置浏览器中打开目标页面。
- 该约定优先于各 Skill 中默认的浏览器启动流程；除非用户当前轮次明确要求 Agent 自行打开浏览器，否则始终按本约定执行。

## 规则优先级

用户当前明确指令 > 不可逆操作与定稿约束 > 模块已确认产品决策 > 本文件其他通用规则 > 专项规则 > Skill 默认行为。

- 用户最新明确指令可更新历史决策，但 Agent 不得仅凭推断、通用规则变化或历史状态覆盖已确认决策。
- 核心规则升级默认不追溯修改既有模块决策；发现冲突时先向用户说明，再确认采用新规则还是保留原决策。
- 永久删除、覆盖原版、迁入 `设计成果/` 等不可逆或交付状态变更，仍须满足对应的显式确认条件。
- 历史状态不能替代用户当前轮次的定稿指令。

## 专项规则索引

| 主题 | 文件 |
|------|------|
| 行业场景库 & AI 用例模板 | `~/.workbuddy/skills/ai-pm-manufacturing/references/` |
| PM 四阶段与接续设计 | `.cursor/rules/pm-workflow.mdc`（如有） |
| 目录与定稿 | `.cursor/rules/project-workspace.mdc`（如有） |
| 需求说明书编写 | 本文件 §需求说明书编写 |
| 版本评审 index | 本文件 §版本评审 index 设计 |
| AI 页面 UI 设计品质规范 | 本文件 §AI 页面 UI 设计品质规范 |
| 设计 Skill 使用指南 | `outputs/AI页面UI设计品质提升指南.md` |
